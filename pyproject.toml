[build-system]
requires = ["setuptools>=45", "wheel", "pybind11"]
build-backend = "setuptools.build_meta"

[project]
name = "ctc-forced-aligner"
version = "0.1.1"
description = "Text to speech alignment using CTC forced alignment (fixed version)"
authors = [{name = "MahmoudAshraf97"}]
license = {text = "MIT"}
requires-python = ">=3.9"
dependencies = [
    "torch",
    "torchaudio",
    "numpy",
    "transformers>=4.41.0",
    "packaging",
]

[project.scripts]
ctc-forced-aligner = "ctc_forced_aligner.align:cli"
